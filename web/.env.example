# Copy to .env.local if you need deploy-specific client values.
# The staging pivot is local-first: core capture, clustering, summaries, and
# settings use browser/extension storage by default.

NEXT_PUBLIC_SITE_URL=http://localhost:3000
